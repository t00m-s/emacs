;;; init-evil.el --- Vim keybindings -*- lexical-binding: t -*-
(use-package evil
             :ensure t
             :init
             (evil-mode 1))
(provide 'init-evil)
;;; init-evil.el ends here
